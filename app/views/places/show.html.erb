<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
  <title>Entries in this Place</title>
</head>

<body class="container">
  <div class="row"> 
    <div class="col-12 pb-4">
        <h1><%= @place["name"] %></h1>
    </div>
  </div>


  <p>
    <a class="btn btn-warning" href="/entries/new?place_id=<%= @place["id"] %>" role="button">New Entry</a>
  </p>

  <div class="row"> 
    <div class="col-12 pb-4">
        <h2>My Entries</h2>
    </div>
  </div>

  <div class="row"> 
    <% for entry in @entries %>
      <div class="col-md-6 col-lg-4 pb-4">
        <h3><%= entry["title"] %></h3>
        <h4><%= entry["occurred_on"] %></h4>
        <p><%= entry["description"] %></p>
        <% if entry.uploaded_image.attached? %>
          <img src="<%= url_for(entry.uploaded_image) %>" class="img-fluid">
        <% else %>
          <img src="<%= entry["image"] %>" class="img-fluid">
        <% end %>
      </div>
    <% end %>
  </div>

  <div class="row"> 
    <div class="col-12 pb-4">
      <p>
        <a class="btn btn-secondary" href="/places">Back to All Places</a>
      </p>
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
</body>
</html>